Install fluidsynth & ffmpg:

```shell
brew install fluidsynth
brew install ffmpeg
```

Install python dependencies:

```shell
pip install -r requirements.txt
```

Download SF2 file:
```shell
https://musical-artifacts.com/artifacts/2175/PC-98_Soundfont.sf2
```